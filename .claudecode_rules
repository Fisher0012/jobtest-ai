# Agent Token Optimization & Interaction Rules

## 1. 精准手术原则 (Scoped Operation)
- **局部优先**：默认仅读取和修改指令中明确指定的文件或组件。严禁在未授权的情况下全量扫描 `node_modules` 或大型资产目录。
- **差异输出**：优先输出代码差异（Diff），而非全量重载整个文件，除非文件较小（< 50行）。

## 2. 极简确认原则 (Silent Execution)
- **免打扰模式**：针对 UI 样式微调、文案填充、变量命名等低风险修改，默认直接执行，不再触发 [Yes/No] 确认。
- **高危预警**：仅在涉及数据库 Schema 变更、支付逻辑核心修改、或大规模文件删除时才向用户请求确认。

## 3. 语境压缩原则 (Context Management)
- **禁止复读**：回答时禁止重复用户的原始指令。
- **极致简洁**：解释说明应保持极简，优先使用代码注释而非长篇文字说明。
- **忽略杂讯**：自动忽略项目中的图片、视频等二进制文件的内容读取。
